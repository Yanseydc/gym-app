export default function DashboardLoading() {
  return (
    <div
      style={{
        padding: 24,
        borderRadius: 24,
        border: "1px solid var(--border)",
        background: "var(--surface)",
      }}
    >
      Loading dashboard...
    </div>
  );
}
